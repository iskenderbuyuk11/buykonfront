# Server-side secrets — never commit real values, never ship to browser

# Gemini (try-on + visual search + Buki — Java /api/ai/* və ya api/buki.php)
GEMINI_API_KEY=
# GEMINI_TRYON_MODEL=gemini-3.1-flash-lite-image
# GEMINI_BUKI_MODEL=gemini-2.0-flash

# Java API
# Lokal: http://localhost:8080/api
# Prod: https://api.buykon.com/api
# API_BASE=

# Spring mail (SpaceMail) — backend application.yml / env
MAIL_HOST=mail.spacemail.com
MAIL_PORT=587
MAIL_USER=buykon@buykon.com
MAIL_PASSWORD=
MAIL_FROM=buykon@buykon.com

# OTP proof (seller register bridge, optional)
# OTP_PROOF_SECRET=
# MAIL_INTERNAL_KEY=
